<Window x:Class="CalculatorClient.CalculatorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CalculatorClient"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="300" Width="300">

    <Grid>
        <StackPanel Orientation="Vertical" >
            <TextBlock Text="{Binding Email}" Height="75"></TextBlock>
            <ListBox ItemsSource="{Binding PermissionSet}" Height="75"></ListBox>
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold">Calculator</TextBlock>
        </StackPanel>
    </Grid>
</Window>
